version: "3"

services:
  server:
    image: hexammon/server-dev
    build:
      context: .
      dockerfile: Dockerfile-server-dev
    volumes:
      - .:/srv/hexammon
    environment:
      - H6N_HOST=server
      - H6N_PORT=9000
      - H6N_ADDR=0.0.0.0
      - H6N_ORIGIN=*

